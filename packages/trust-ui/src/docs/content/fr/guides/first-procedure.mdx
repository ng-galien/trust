---
title: "Écrire et publier une Procédure"
summary: "De Nouvelle procédure à une version publiée — rôles de contexte, Scénarios, Checks, qualifications, graphe."
order: 2
---

<Steps>
  <Step title="Ouvrir le catalogue et créer">Barre latérale → **Procédures** → **Nouvelle procédure**. L'onglet Source s'ouvre sur un modèle.</Step>
  <Step title="La nommer">Renseignez `@procedure:<slug>` et `@version:1.0.0` ; gardez `@trust-dsl:1`. Donnez-lui un titre et, dessous, quelques lignes décrivant ce qu'elle établit.</Step>
  <Step title="Déclarer le périmètre et le contexte">Commencer `Background: Plan context` par la table obligatoire `Given Procedure scope`, avec une ligne `all` en prose autorisée et interdite. Déclarer ensuite une ligne par rôle : ce que fournit l'engagement, ce qu'un Check remplira, ce que l'agent déclare, ce qui est fixe.</Step>
  <Step title="Écrire le premier Scénario">`@scenario:<slug>`, un titre, puis un Check : `Then Check "…" runs Operation "…" on "<role>" as Input "<input>" … and must establish "…"`. Utilisez la complétion **Check** ou **Use `<operation>`** : les noms d'Opération viennent du catalogue.</Step>
  <Step title="Écrire la qualification">Attachez un DocString `js` au Check. Écrivez chaque garde sous la forme `condition || fail(reason)` ; joignez les gardes indépendantes avec `&&`.</Step>
  <Step title="Enchaîner les Scénarios">Scénario suivant : `Given scenario "<slug>" is validated` d'abord, puis ses Checks. Liez les rôles matérialisés avec `using "<role>"` ; lisez un Check antérieur par `checks["<check>"].field` dans la qualification.</Step>
  <Step title="Lire le graphe">Onglet Graphe : vérifiez les liens d'ordre et les liens de données en pointillés ; sélectionnez un Check pour voir ce dont il a besoin et ce qu'il réinitialiserait.</Step>
  <Step title="Simuler, puis publier">Onglet Simuler pour jouer l'ordre des Scénarios. **Publier** stocke la version ; un Plan ou un dry-run peut être engagé depuis elle.</Step>
</Steps>

Détails : [Anatomie d'une Procédure](/docs/procedures/anatomy) · [Procédures dans l'interface](/docs/procedures/authoring).
