{
  "contract": "trust.compiled-operation@1",
  "operation": "http.status-read",
  "version": "1.0.0",
  "title": "Read service status",
  "input": {
    "type": "object",
    "properties": {},
    "required": [],
    "additionalProperties": false
  },
  "environment": {
    "type": "object",
    "properties": {
      "serviceUrl": {
        "type": "string",
        "format": "trust-url"
      }
    },
    "required": [
      "serviceUrl"
    ],
    "additionalProperties": false
  },
  "steps": [
    {
      "name": "response",
      "type": "http",
      "http": {
        "url": {
          "environment": "serviceUrl"
        },
        "format": "json"
      }
    }
  ],
  "produce": {
    "language": "jsonata",
    "expression": "{ \"service\": steps.response.body.service, \"status\": steps.response.status }"
  },
  "produced": {
    "type": "object",
    "properties": {
      "service": {
        "type": "string"
      },
      "status": {
        "type": "number"
      }
    },
    "required": [
      "service",
      "status"
    ],
    "additionalProperties": false
  }
}
